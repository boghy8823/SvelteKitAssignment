# Signs the session cookie (HMAC-SHA256). Any long random string will do.
#
# In dev, a documented fallback is used when this is unset so a fresh clone runs
# with no setup. Outside dev the app refuses to sign a session without it, rather
# than quietly using a value that is published in the repository.
#
#   openssl rand -base64 32
AUTH_SECRET=
